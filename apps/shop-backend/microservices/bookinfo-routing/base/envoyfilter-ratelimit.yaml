# v2
# 本地限流（Istio 1.30 官方 Rate Limit 任务页 "Local rate limiting" 示例）：
# 在 productpage sidecar 的 inbound 链路上插入 envoy.filters.http.local_ratelimit。
# v2 配额从官方演示值 4 个/分钟 调到 60 个/分钟：一个页面加载就要
# 4~6 个请求（页面 + 静态资源），4/分钟连正常浏览都扛不住（页面刚
# 打开就会 429、资源加载不全）；60/分钟正常浏览无感，连点仍可复现
# 429 与 x-local-rate-limit: "true" 响应头。官方演示值 4/分钟适合
# 受控演示，复现方法见 docs/istio-resilience.md。
#
# 两个关键边界（详见 docs/istio-resilience.md）：
# 1. 本地限流按 Envoy 实例独立计数：Pod 数越多总配额越高，不是全局精确限流；
#    全局限流需部署官方 ratelimit 服务（ConfigMap 描述符 + EnvoyFilter 联动）。
# 2. EnvoyFilter 只作用于同命名空间的 workload：本文件随 routing overlay
#    渲染进 dev/staging/prod，各自只限制本环境 app=productpage 的 sidecar，
#    三个环境配额互相独立（官方 EF 参考：root namespace 才全域生效）。
apiVersion: networking.istio.io/v1alpha3
kind: EnvoyFilter
metadata:
  name: filter-local-ratelimit-productpage
spec:
  workloadSelector:
    labels:
      app: productpage                      # 只注入 productpage 的 sidecar（各环境一份）
  configPatches:
    - applyTo: HTTP_FILTER
      match:
        context: SIDECAR_INBOUND
        listener:
          filterChain:
            filter:
              name: "envoy.filters.network.http_connection_manager"
      patch:
        operation: INSERT_BEFORE
        value:
          name: envoy.filters.http.local_ratelimit
          typed_config:
            "@type": type.googleapis.com/udpa.type.v1.TypedStruct
            type_url: type.googleapis.com/envoy.extensions.filters.http.local_ratelimit.v3.LocalRateLimit
            value:
              stat_prefix: http_local_rate_limiter
              token_bucket:
                max_tokens: 60               # 桶容量 60 个 token（官方演示值 4）
                tokens_per_fill: 60          # 每 fill_interval 补充 60 个
                fill_interval: 60s
              filter_enabled:
                runtime_key: local_rate_limit_enabled
                default_value:
                  numerator: 100
                  denominator: HUNDRED
              filter_enforced:
                runtime_key: local_rate_limit_enforced
                default_value:
                  numerator: 100
                  denominator: HUNDRED
              response_headers_to_add:
                - append: false
                  header:
                    key: x-local-rate-limit
                    value: "true"            # 被限流的响应带上此头，便于识别与测试
