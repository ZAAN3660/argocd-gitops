# v8.1（骨架环境：放开 staging 但副本数 0）
# Rollout replicas=0：环境骨架先就位，不起 Pod；
# ScaledObject minReplicaCount 同步设为 0：否则 KEDA 会把副本拉回 1。
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: staging
resources:
  - ../../base
  - virtual-service.yaml
images:
  - name: docker.io/istio/examples-bookinfo-details-v1
    newTag: 1.16.2
patches:
  - target:
      kind: Rollout
      name: details
    patch: |-
      - op: replace
        path: /spec/replicas
        value: 0
  - target:
      kind: ScaledObject
      name: details
    patch: |-
      - op: replace
        path: /spec/minReplicaCount
        value: 0
  - target:
      kind: DestinationRule
      name: details
    patch: |-
      - op: replace
        path: /spec/host
        value: details.staging.svc.cluster.local
