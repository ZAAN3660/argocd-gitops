apiVersion: v1
kind: Service
metadata:
  name: ratings
  labels:
    app: ratings
spec:
  type: ClusterIP
  selector:
    app: ratings
  ports:
    - name: http
      port: 9080
      targetPort: 9080
      protocol: TCP
