apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
helmCharts:
  - name: opentelemetry-operator
    repo: https://open-telemetry.github.io/opentelemetry-helm-charts
    version: 0.122.0
    releaseName: opentelemetry-operator
    namespace: opentelemetry-operator
    valuesFile: values.yaml
# resources:
# 2026-08-25 退役 otel-agent-rbac.yaml（已删文件）：旧 CR 的 prometheus receiver 用
# kubernetes_sd 需要 pods list/watch 补权；agent/gateway 拆分后服务发现移交
# target_allocator（operator 自动创建 TA 的 SA/RBAC，含 pods 权限），collector 自身
# 不再需要 K8s API 权限。此补权对象从本 kustomization 移除，由 infra App prune 回收。
