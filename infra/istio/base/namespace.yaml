# istio-system 命名空间声明：istio.io/rev 标签在此声明（因果链见 kustomization.yaml 注释）。
# Delete=false：App 被删/重装时不级联删除本 ns，避免连带清空 istiod/gateway。
apiVersion: v1
kind: Namespace
metadata:
  name: istio-system
  labels:
    istio.io/rev: default
  annotations:
    argocd.argoproj.io/sync-options: Delete=false
